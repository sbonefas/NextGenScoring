const drw = require('../data_read_write');
const fs = require('fs');

describe('new test file', function() {
     it('should properly transform a string of stats to a 2D array', function() {
         drw.test_delete_file("string");
     });
   });
   